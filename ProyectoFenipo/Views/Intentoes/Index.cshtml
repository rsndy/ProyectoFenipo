@model IEnumerable<ProyectoFenipo.Models.Intento>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Movimiento)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NumeroIntento)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.KilosMovimiento)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Estatus)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.InscripcionAtletas.Id)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Movimiento)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NumeroIntento)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.KilosMovimiento)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Estatus)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.InscripcionAtletas.Id)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
