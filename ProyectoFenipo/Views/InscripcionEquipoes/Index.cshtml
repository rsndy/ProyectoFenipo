@model IEnumerable<ProyectoFenipo.Models.InscripcionEquipo>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DelegadoEquipo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Equipo.NombreEquipo)
        </th>
        <th>
            @*@Html.DisplayNameFor(model => model.Competencia.Nombre)*@
            @Html.DisplayName("Competencia")
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DelegadoEquipo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Equipo.NombreEquipo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Competencia.Nombre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
